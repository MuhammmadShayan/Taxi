export const metadata = {
  title: 'Customer - KiraStay',
  description: 'Customer dashboard',
};

export default function CustomerLayout({ children }) {
  return (
    <div className="customer-layout">
      {children}
    </div>
  );
}


